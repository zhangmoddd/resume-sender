# 修复记录（2026-09-17）

一次针对"用久了才会发现"的问题的排查与修复。改动只涉及 `app.py`、`index.html`、`README.md`，
数据结构没变，你 `data/` 里已有的清单、记录、附件都不会丢。

改完之后自测过了 **86 项断言全通过**（含真起服务打接口、并发写入、附件拦截、文件读坏恢复），
并用无头 Chrome 真加载了一遍页面，控制台零报错。

---

## 一、严重：以前会真出事的

### 1. 投递过程中改清单，改动会被后台覆盖
- **以前**：一边发邮件一边改进展、写备注、打标签，过几十秒被"发信那个后台"覆盖回去；
  删掉的公司会重新冒出来，而且**照样把邮件发给它**；新加的公司会自己消失。
  正在格子里打字时也会被刷掉。
- **现在**：后台每发一封都重新读一次最新清单，只改自己那一条。
  发送期间你可以随便改进展、备注、标签、删条目、加公司 —— 都不会被覆盖，
  删掉的目标**不会再发出去**，也不会复活。你正在打字的格子不会被刷掉。

### 2. 附件可能发错，甚至根本不带附件
- **以前**：用「在线填表 / Excel 导入」进来的公司，发送时会带上附件库里**全部**文件
  （包括给别家准备的压缩包、另一版简历）；如果你把某个附件删了、或重新上传过
  （名字变成 `xxx(1).pdf`），那家公司发出去就**一个附件都没有**，状态还显示"已发送"，
  你完全看不出来。
- **现在**：清单里多了一列**附件**，直接写清"全局·N个"或"⚡ 附件×2"；附件库为空时显示红色的「⚠ 没附件」。
  点「开始投递」前软件会先检查：附件对不上的**先拦下来、一封都不发**，
  并列出是哪几家、缺哪个文件。你也可以选"只投其余能发的"，有问题的那几条会被标成
  「失败」+ 写明原因，补好附件后可以单独补投。

### 3. 同一批邮件可能被完整发两遍
- **以前**：点「开始投递」→ 确认 → 手快再点一次，可能同时跑两个发送任务，每家投两遍。
  黑色窗口没关就再双击一次 Start.bat，会开第二个软件、共用同一份数据，同样会重复发。
- **现在**：启动投递这一步加了锁，只有一个请求能成功，另一个会收到"已有投递任务在进行中"。
  软件同时**只能开一个** —— 再双击 Start.bat 不会开第二个，而是把已经开着的那个页面打开给你。

### 4. 极端情况下清单会"看起来全没了"，再点一下真丢
- **以前**：文件万一被写坏（两个标签页同时操作、断电、磁盘故障），软件当成"空清单"显示，
  还不报错；这时你随手加一条公司，**原文件就被覆盖，再也找不回**。
- **现在**：文件内容读不出来时，软件**先把原文件改名备份**成 `xxx.json.corrupt-日期时间` 放在 `data/` 里，
  页面顶部大字提醒你怎么恢复，绝不当场覆盖。所有保存都是"先写临时文件再改名"，
  临时文件名带进程和线程号，多个操作同时保存也不会互相踩、不会写出坏文件。

## 二、中等：会让人用错、看错

### 5. 改了模板没点「保存这套」就发，发出去的是旧话术
- **以前**：预览显示的是你刚改的新内容，实际发出去的是磁盘上旧的；刷新页面改动直接没。
- **现在**：切到别的模板 / 切到别的标签页 / 直接点投递，都会先把改动自动存一下；
  编辑框旁边有红字「● 有改动没保存」；真要关页面会提醒你一句。

### 6. 投递中刷新页面，界面上什么都看不到
- **以前**：进度条、日志、停止按钮全没，你会以为没在发，然后去关黑窗口（等于中断整批）。
- **现在**：刷新后自动把进度和日志接回来，顶部还会显示「· 投递中…」。

### 7. 首页统计数字会算错
- **以前**：给一家还没投的公司标上"面试"，首页会显示"已投 0 家、面试 1"，前后对不上；
  "我放弃"也没算进去，未回音可能是负数。
- **现在**：一律只统计**已经投出去**的那批，并补上了"挂了"和"我放弃"。

### 8. 「重要日期」提醒永远差一天
- **以前**：约的是今天，显示"还有 1 天"（"就是今天"这句永远出不来）。
- **现在**：按日历天算，今天显示"（就是今天）"，明天显示"（还有 1 天）"。

## 三、顺手一起修的

- **发送日志跨轮次串场**：第二轮投递的日志框里混着上一轮的内容 → 每轮开始前清空；
  日志也不再只给最后 30 条（投 60 封也看得全）。
- **设置清不掉**：以前把"发件人显示名/姓名/学校"清空，看着是空的、后端其实还是旧值 →
  现在真的能清掉；保存后输入框按真实值回填，"间隔"填了非数字也不会把设置写坏。
- **接口出错时清空界面**：以前后端报错，界面会显示成"还没有投递目标/还没有投递记录"，
  让人以为数据丢了 → 现在读失败会保留原有显示并提示，绝不假装成空数据。
- **中文顿号不算分隔符**：标签写"国企、内推"以前会变成一个超长标签 → 现在顿号、竖线都当分隔符。
- **多行正文被吃掉**：备注正文里的换行/缩进以前被首尾去空格 → 现在原样保留。
- **在线填表草稿越存越大**：每打开一次都会多存 10 行空白（一路涨到 400 行）→ 现在只存有内容的部分。
- **发信线程没有兜底**：任何没预想到的异常会让整批静默中断、那条永远停在"发送中" →
  现在单封出错只算那一封失败，日志写明；卡在"发送中"的条目下次投递会被放回"待发"。
- **收件邮箱为空/格式不对**：以前可能发出一封没有收件人的邮件 → 现在直接判为失败并写明。

---

## 自测覆盖了什么

- 发信过程中改进展/标签/笔记 → 不被覆盖
- 发信过程中删掉的目标 → 不发、不复活
- 附件缺失 / 附件库为空 → 拦下、不静默发送
- 两个并发「开始投递」→ 只接受一个，每封只发一次
- 文件读坏 → 备份 + 警告 + 不覆盖
- 6 个线程同时写 150 次 → 文件仍是合法 JSON、一条不丢、不留临时文件
- 第二个软件实例 → 拿不到锁
- 清单增删改、打标签、Excel 导入导出、在线填表导入、附件上传删除、PDF 合并 → 全部正常
- 用无头 Chrome 真加载页面 → 渲染正常、控制台零报错

> 唯一没变的老规矩：**只投你确实要投的公司**，别把间隔改到 30 秒以下。

---

## 补充：把发送节奏的"安全绳"系紧了

起因是一个很自然的问题：**"一秒发一封，应该没事吧？我又不是打广告。"**

答案是**不行，而且一秒一封基本等于自曝**。改了三处：

1. **间隔下限从 5 秒提到 20 秒**（写入时拦 + 发送时再拦一遍，就算手改 config.json 也没用）
2. **「发送设置」页加了实时提示**：间隔低于 30 秒、或每日上限超过 100 封，会当场红字警告并说明后果
3. **投递前的确认框**里也会提醒；README 新增一节《为什么不能发太快》，把原因和后果写清楚

### 为什么这件事这么要紧

邮箱**看不懂你在投简历**。它看不到诚意，只看行为：

> 一个账号 + 短时间内 + 发给一堆互不相识的收件人 + 内容高度相似 + 带附件

—— 这就是垃圾邮件的标准画像。**投简历和打广告，在邮箱眼里长得一模一样。**

- 腾讯帮助页写的是"访问频率过高会限制服务"，**没公开确切数字**（网上流传 100/200/500 封都有，
  说明是按账号动态判定的）。所以"官方没说"≠"没事"，而是你根本不知道自己离红线多远。
- 一秒一封 = **一小时 3600 封**。这不是"会不会被发现"，是头几封之后就会被拦。
- 公开有人反馈：**一连发 9 封，授权码当场失效**（报 `535 Login Fail`）。

### 被拦之后会怎样

| 情况 | 表现 | 恢复 |
|---|---|---|
| 临时限流（最常见） | 之后全部失败、提示「登录失败」 | 等 1~2 小时 |
| 授权码作废 | 突然报 535 登录失败 | 重新生成，可能要等一天 |
| 服务被停用 | SMTP 服务被关 | 要重新开通，最麻烦 |

**还有一笔更常见、也更亏的隐形损失：HR 那边压根收不到。**
收件人里很多是 163、企业邮箱，它们对"陌生发件人 + 短时间大量 + 带附件"判得更狠，
直接丢垃圾箱或拒收。**你这边显示「发送成功」，人家那边什么也没看见** ——
比被限流更常见，而且你完全察觉不到。

### 正确的做法

- 保持默认：**40 秒一封、一天 50 封**（这是刻意压到很低的，不是随便定的）
- 想多投几十家 → **分几天投**，而不是把间隔改小
- 一天 50～100 封是安全区，100 封以上随时可能被限

---

## 第二轮：另一位 agent 复查后，我又核了一遍

他报了 6 条问题。我**逐条真跑了一遍去复核**（不是看代码点头），结论：**6 条全部属实，没有误报**。
其中第 2 条是我上一轮改动留下的洞，先认下来。

| 他报的问题 | 我怎么复核的 | 我的严重度判断 |
|---|---|---|
| 1 点「编辑」再保存，附件从"全带"变成固定名单 | 真起服务走了一遍完整流程 | **中**：静默发生，而且同一份清单两套脾气 |
| 2 配置里数字不是数字 → 投递卡死，只能重启 | 真把 interval 改成空字符串跑了一次 | **中高**：但要把配置文件手改坏才会触发 |
| 3 第二个实例会指到错端口 | 开两个进程实测读锁文件 | 低：只在 8765 被别的程序占着时 |
| 4 光标在开头时，变量插到末尾 | 看代码 + JS 实测 | 低：烦人，但好绕开 |
| 5 atts 传字符串被按字拆 / 缺 id 让接口 500 | 直接打接口 | 很低：前端不会这么传 |
| 6 SMTP 超时 30 秒偏短 | 看代码 + 想清楚超时语义 | 低：方向对，但理由不准确 |

### 第 6 条他说得不严谨
Python 的 socket 超时是"**单次收发 30 秒没动静**"，不是"整封邮件总共只能发 30 秒"。
所以边传边有进展的大附件，通常不会超时。真正会出问题的是服务器卡住不回话 ——
而"邮件其实已经发出去了、这边却记成失败"是 SMTP 本身的性质（最后那个 250 应答丢了就说不清），
跟超时设 30 还是 90 关系不大。不过放宽到 90 秒**没有坏处**，已经改了。

### 他说"验过没问题"的那条，我也验了
「收件邮箱里塞换行不会偷偷加 Bcc」——**结论是对的**。我额外测了两种情形：

- 带换行的原样交给邮件库 → 邮件库直接抛 `HeaderParseError` 拒发，不会注入；
- 换行被清掉后变成 `hr@a.comBcc:victim@b.com` → 地址解析器返回空、收件人为空 →
  服务器拒收报错，**也不会发给陌生人**。

所以确实没有 Bcc 注入。但它会以一堆看不懂的英文报错收场，
所以我顺手加了一层邮箱校验：冒号、分号、尖括号、换行这类不可能出现在邮箱里的字符，
当场拒绝并说清是哪几个字符。

### 这一轮改了什么

1. **编辑框里加了明确的「跟着附件库走」勾选项**（清单里显示「全局」）：
   勾上 = 附件库里有什么就全带上、**以后新上传的也自动带上**；
   取消勾选 = 只发你亲手勾的那几个（固定名单）。两种意思从此分得清、看得见。
2. **`sender_worker` 整个函数体包进 try** —— 配置坏了也只是用默认值继续发，
   不会再把界面永久卡在「投递中」（以前卡住后点开始、点停止都救不回来，只能重启软件）。
3. **第二个实例改成挨个端口问一句"你是谁"**（认 Server 头），
   不再依赖读那个被字节锁住的锁文件 —— Windows 上那个文件根本读不出来。
4. **`insertVar` 修掉 `0 || 长度` 的经典坑**：光标在开头时不再插到末尾。
5. **`atts` 传字符串不再被按字拆开**；`/api/preview` 遇到缺 id 的脏数据不再返回 500。
6. **SMTP 超时 30 → 90 秒**。
7. **邮箱规整与校验**（见上）。

### 验证结果

- 修复 1/2/3/5/6 + 邮箱校验：**逐条实测通过**
- 回归：附件缺失仍然拦下、发信期间改清单不被覆盖、并发点两次只发一遍 —— 全部仍然成立
- 你的 9 条清单、4 条已发送记录一个字节没动

---

## 第三轮：他又查了一遍（以当前代码为准）

这轮他报了 5 条。我逐条实测：**①②③⑤ 属实，④我没能复现**。

| 他报的问题 | 复核结果 | 严重度 |
|---|---|---|
| ① data 文件里混进"结构不对"的项 → 一串接口 500 | **属实**（但有处归错了：`/api/jobs/history` 只在投递记录文件脏时才炸，清单脏不影响它） | 中 |
| ② 在线填表打开 Excel，超 400 行静默丢 | **属实**（450 行只铺 400 行，接口也不告诉前端少了） | 中低 |
| ③ 前端让选 .xls，后端只认 .xlsx | **属实**（弹的是英文 `File is not a zip file`） | 中低 |
| ④ 手动新增的 id 在同一毫秒会重复 | 代码上确实会撞，**但走网页撞不上**（实测每次请求间隔 15～40 毫秒，连加 6 条 id 都不同） | 极低 |
| ⑤ 合并挪顺序中途崩了会留个垃圾文件 | **属实**（列表里会冒出一条"格式不认识"的条目） | 极低 |

### 这一轮改了什么

1. **读清单/读记录时只保留结构正确的条目**（`get_jobs()` / 新增 `get_sent_log()`）。
   文件被手改坏、磁盘出问题时，不再是投递、打标签、导出、在线填表一起报 500 +
   `'str' object has no attribute 'get'` 这种天书，而是自动忽略坏的那一项，
   并在界面顶部给一句人话警告。
2. **「在线填表」里打开 Excel 超过 400 行不再静默截断**：接口把"原文件共多少行""上限多少"
   带回来，前端弹窗说清少了多少行，并告诉你去「📂 导入 Excel」（那条路没有行数上限）。
3. **文件选择框不再让选 .xls**；真选了老 .xls（或者别的文件），会明确告诉你
   「在 Excel 里另存为 .xlsx 再导」，不再甩英文错误。
4. **新增条目的 id 撞了就顺延**。实测 8 个线程并发新增 40 条、再导入 300 行，id 全部唯一。
5. **合并挪顺序的中转文件改名为 `_swap.tmp`**（不带 `__`，不会被当成待合并文件列出来），
   清空列表时顺手清掉。

### 验证

- ① 脏数据下 7 个接口全部正常，界面有警告
- ② 400 行 / 原文件 450 行 / "超了"标记 都正确带回；「导入 Excel」450 条全进
- ③ 老 .xls 和随便一个文件，提示都是人话
- ④ 并发新增 40 条 + 导入 300 条，id 无重复
- ⑤ 残留的中转文件不再出现在列表里；挪顺序本身仍然正常
- 回归：附件缺失拦截、怪字符邮箱被拒、发信期间改清单不被覆盖 —— 全部仍然成立

### 他这轮的一个态度值得记一下

他主动说明了自己上一轮"只读了 app.py 前 58 行就转去跑测试，后面的结论是靠印象推的"，
并且承认了 SMTP 超时那条的解释我说得对。**报问题时愿意自己回头修正，比一次报得多有用。**

---

## 第四轮：编辑弹窗里"跟着附件库走"那行文字被挤成竖条（界面）

**你看到的现象**：点「编辑」→ 往下滑到附件那一块，勾选框旁边空一大片，
说明文字变成**一个一个字竖着排**的一条，弹窗底下还多出一根**横向滚动条**。

**根因**（跟数据无关，纯粹是 CSS 一行没写对）：

`.field input { width: 100% }` 这条规则本意是"输入框占满整行"，
但它把这个弹窗里的**勾选框**也拉成了整行宽（实测 516px）。
勾选框在那行里又写了 `flex:none`（不许缩），于是它一个就占满了一行，
把旁边的说明文字挤到只剩 52px 宽 → 中文没有空格，硬挤就变成**一个字一行**的竖条（高 444px），
并且整行比弹窗还宽 → 弹窗出现横向滚动条。

**改法**：`index.html` 的 CSS 里补一条，把勾选框/单选框从"占满整行"里排除掉：

```css
.field input[type=checkbox], .field input[type=radio] { width: auto; height: auto; padding: 0; border: none; border-radius: 0; }
```

**验证**（用无头 Edge 真加载页面、把弹窗打开量的尺寸）：

| 指标 | 修之前 | 修之后 |
|---|---|---|
| 弹窗内容区 宽 / 内容实际宽 | 560 / **598**（溢出 38） | 560 / **560**（不溢出） |
| 勾选框宽度 | **516**（整行） | 13 |
| 说明文字那块 | 宽 **52**、高 444（竖条） | 宽 **495**、高 70（正常两行） |

只动了 `index.html` 一个文件、一行 CSS，`app.py`、`data/` 都没碰。
全项目只有这一处勾选框在 `.field` 里面（其余在表格里），所以只有这一处中招。

---

## 第五轮：Start.bat 里写死的本机路径（启动脚本）

**报的问题**：另一位 agent 审代码时发现 `Start.bat` 第 7 行写死了作者自己电脑上的 Python 路径
（形如 `%USERPROFILE%\<某工具目录>\binaries\python\envs\default\...`），而 README 里写着"已去除本机路径"。
它说得对。但它给的解法（"找不到就退回系统的 python"）没解决真正的问题。

**先把本机四个解释器挨个 import 一遍**：

| 解释器 | openpyxl | pypdf | PIL | tkinter |
|---|---|---|---|---|
| 本机 A：一个工作用的 venv | 有 | 有 | 有 | **没有** |
| 本机 B：`python`（PATH 上那个，conda 装的） | **没有** | 有 | 有 | 有 |
| 本机 B 同一份，用 `py` 启动器调起来 | 没有 | 有 | 有 | 有 |

于是得到两条结论，都不在"排序"这个层面上：

1. **主程序要 openpyxl，这台机器上只有那个 venv 有。** 所以"把用户自己的 python 排第一"会让软件
   **能启动、一点导出 Excel 就炸** —— 光看"文件在不在"是不够的。
2. **合并工具要 tkinter，那个 venv 正好没有。** 两个脚本要的**不是同一个解释器**，
   不能共用一个"挑 Python"的结果。这也解释了为什么 app.py 里要单独写一个 `find_gui_python()`。

**改法**

1. **不再"看文件在不在"，改成真 import 试一次**：候选必须
   `python -c "import openpyxl, pypdf, PIL"` 返回 0 才采用（合并工具那边试的是 `tkinter, pypdf`）。
2. **顺序改成用户优先**：`data\local_python.txt` → 项目内 `.venv` → PATH 上的 `python` / `py`
   → 常见安装位置（miniconda / anaconda / python.org 默认目录）→ 最后一个能跑的 python（带警告）。
3. **写死的作者路径从两个 .bat 里全部删掉**，改写到 `data\local_python.txt`
   （这个文件本来就是干这个用的，而且已排除在 Git 之外）。
4. 加 `--probe`：`Start.bat --probe` 只打印它挑中了哪个解释器，不启动软件，方便以后排查。
5. 一个都找不到时，打印人话（去哪装、装什么、也可以手写路径），而不是黑窗口一闪就没了。

**顺手修掉的一个隐藏坑**：两个 .bat 之前都是 **LF 换行**（Unix 风格），
Windows 的 cmd 读这种文件会串行 —— 实测 `合并PDF.bat` 里那些英文注释被 cmd 当成命令去执行了 9 次
（`'le' 不是内部或外部命令`、`FIND: 参数格式不正确`、`'Order:' 不是内部或外部命令`…）。
起因是文件里还有一行 UTF-8 中文，被 cmd 按 GBK 读之后把后面的行一起带错。
两处都改成 **CRLF + 纯 ASCII**（**.bat 里一个中文都不写**）。

**验证**（真跑 bat，不是看代码点头）：

```
> Start.bat --probe
Selected Python: <本机 A 的 python.exe>          ← 有 openpyxl，对

> 合并PDF.bat --probe
Selected Python: <本机 B 的 python.exe>
Window-less exe : <本机 B 的 pythonw.exe>         ← 有 tkinter + pypdf，对
```

两边零报错，各挑各的，`local_python.txt` 里两行路径被正确地区分开用。
改完之后全项目搜一遍：本机用户名、工具目录、具体盘符路径 —— 一个残留都没有了。

**留给以后的提醒**：以后用工具编辑 .bat，一定确认两件事 —— 换行是 **CRLF**、内容里**别有中文**。
这次就是这两样凑到一起才炸的。

