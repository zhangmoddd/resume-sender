# 修复记录（2026-09-17）

一次针对"用久了才会发现"的问题的排查与修复。改动只涉及 `app.py`、`index.html`、`README.md`，
数据结构没变，你 `data/` 里已有的清单、记录、附件都不会丢。

改完之后自测过了 **86 项断言全通过**（含真起服务打接口、并发写入、附件拦截、文件读坏恢复），
并用无头 Chrome 真加载了一遍页面，控制台零报错。

---

## 一、严重：以前会真出事的

### 1. 投递过程中改清单，改动会被后台覆盖
- **以前**：一边发邮件一边改进展、写备注、打标签，过几十秒被"发信那个后台"覆盖回去；
  删掉的公司会重新冒出来，而且**照样把邮件发给它**；新加的公司会自己消失。
  正在格子里打字时也会被刷掉。
- **现在**：后台每发一封都重新读一次最新清单，只改自己那一条。
  发送期间你可以随便改进展、备注、标签、删条目、加公司 —— 都不会被覆盖，
  删掉的目标**不会再发出去**，也不会复活。你正在打字的格子不会被刷掉。

### 2. 附件可能发错，甚至根本不带附件
- **以前**：用「在线填表 / Excel 导入」进来的公司，发送时会带上附件库里**全部**文件
  （包括给别家准备的压缩包、另一版简历）；如果你把某个附件删了、或重新上传过
  （名字变成 `xxx(1).pdf`），那家公司发出去就**一个附件都没有**，状态还显示"已发送"，
  你完全看不出来。
- **现在**：清单里多了一列**附件**，直接写清"全局·N个"或"⚡ 附件×2"；附件库为空时显示红色的「⚠ 没附件」。
  点「开始投递」前软件会先检查：附件对不上的**先拦下来、一封都不发**，
  并列出是哪几家、缺哪个文件。你也可以选"只投其余能发的"，有问题的那几条会被标成
  「失败」+ 写明原因，补好附件后可以单独补投。

### 3. 同一批邮件可能被完整发两遍
- **以前**：点「开始投递」→ 确认 → 手快再点一次，可能同时跑两个发送任务，每家投两遍。
  黑色窗口没关就再双击一次 Start.bat，会开第二个软件、共用同一份数据，同样会重复发。
- **现在**：启动投递这一步加了锁，只有一个请求能成功，另一个会收到"已有投递任务在进行中"。
  软件同时**只能开一个** —— 再双击 Start.bat 不会开第二个，而是把已经开着的那个页面打开给你。

### 4. 极端情况下清单会"看起来全没了"，再点一下真丢
- **以前**：文件万一被写坏（两个标签页同时操作、断电、磁盘故障），软件当成"空清单"显示，
  还不报错；这时你随手加一条公司，**原文件就被覆盖，再也找不回**。
- **现在**：文件内容读不出来时，软件**先把原文件改名备份**成 `xxx.json.corrupt-日期时间` 放在 `data/` 里，
  页面顶部大字提醒你怎么恢复，绝不当场覆盖。所有保存都是"先写临时文件再改名"，
  临时文件名带进程和线程号，多个操作同时保存也不会互相踩、不会写出坏文件。

## 二、中等：会让人用错、看错

### 5. 改了模板没点「保存这套」就发，发出去的是旧话术
- **以前**：预览显示的是你刚改的新内容，实际发出去的是磁盘上旧的；刷新页面改动直接没。
- **现在**：切到别的模板 / 切到别的标签页 / 直接点投递，都会先把改动自动存一下；
  编辑框旁边有红字「● 有改动没保存」；真要关页面会提醒你一句。

### 6. 投递中刷新页面，界面上什么都看不到
- **以前**：进度条、日志、停止按钮全没，你会以为没在发，然后去关黑窗口（等于中断整批）。
- **现在**：刷新后自动把进度和日志接回来，顶部还会显示「· 投递中…」。

### 7. 首页统计数字会算错
- **以前**：给一家还没投的公司标上"面试"，首页会显示"已投 0 家、面试 1"，前后对不上；
  "我放弃"也没算进去，未回音可能是负数。
- **现在**：一律只统计**已经投出去**的那批，并补上了"挂了"和"我放弃"。

### 8. 「重要日期」提醒永远差一天
- **以前**：约的是今天，显示"还有 1 天"（"就是今天"这句永远出不来）。
- **现在**：按日历天算，今天显示"（就是今天）"，明天显示"（还有 1 天）"。

## 三、顺手一起修的

- **发送日志跨轮次串场**：第二轮投递的日志框里混着上一轮的内容 → 每轮开始前清空；
  日志也不再只给最后 30 条（投 60 封也看得全）。
- **设置清不掉**：以前把"发件人显示名/姓名/学校"清空，看着是空的、后端其实还是旧值 →
  现在真的能清掉；保存后输入框按真实值回填，"间隔"填了非数字也不会把设置写坏。
- **接口出错时清空界面**：以前后端报错，界面会显示成"还没有投递目标/还没有投递记录"，
  让人以为数据丢了 → 现在读失败会保留原有显示并提示，绝不假装成空数据。
- **中文顿号不算分隔符**：标签写"国企、内推"以前会变成一个超长标签 → 现在顿号、竖线都当分隔符。
- **多行正文被吃掉**：备注正文里的换行/缩进以前被首尾去空格 → 现在原样保留。
- **在线填表草稿越存越大**：每打开一次都会多存 10 行空白（一路涨到 400 行）→ 现在只存有内容的部分。
- **发信线程没有兜底**：任何没预想到的异常会让整批静默中断、那条永远停在"发送中" →
  现在单封出错只算那一封失败，日志写明；卡在"发送中"的条目下次投递会被放回"待发"。
- **收件邮箱为空/格式不对**：以前可能发出一封没有收件人的邮件 → 现在直接判为失败并写明。

---

## 自测覆盖了什么

- 发信过程中改进展/标签/笔记 → 不被覆盖
- 发信过程中删掉的目标 → 不发、不复活
- 附件缺失 / 附件库为空 → 拦下、不静默发送
- 两个并发「开始投递」→ 只接受一个，每封只发一次
- 文件读坏 → 备份 + 警告 + 不覆盖
- 6 个线程同时写 150 次 → 文件仍是合法 JSON、一条不丢、不留临时文件
- 第二个软件实例 → 拿不到锁
- 清单增删改、打标签、Excel 导入导出、在线填表导入、附件上传删除、PDF 合并 → 全部正常
- 用无头 Chrome 真加载页面 → 渲染正常、控制台零报错

> 唯一没变的老规矩：**只投你确实要投的公司**，别把间隔改到 30 秒以下。

---

## 补充：把发送节奏的"安全绳"系紧了

起因是一个很自然的问题：**"一秒发一封，应该没事吧？我又不是打广告。"**

答案是**不行，而且一秒一封基本等于自曝**。改了三处：

1. **间隔下限从 5 秒提到 20 秒**（写入时拦 + 发送时再拦一遍，就算手改 config.json 也没用）
2. **「发送设置」页加了实时提示**：间隔低于 30 秒、或每日上限超过 100 封，会当场红字警告并说明后果
3. **投递前的确认框**里也会提醒；README 新增一节《为什么不能发太快》，把原因和后果写清楚

### 为什么这件事这么要紧

邮箱**看不懂你在投简历**。它看不到诚意，只看行为：

> 一个账号 + 短时间内 + 发给一堆互不相识的收件人 + 内容高度相似 + 带附件

—— 这就是垃圾邮件的标准画像。**投简历和打广告，在邮箱眼里长得一模一样。**

- 腾讯帮助页写的是"访问频率过高会限制服务"，**没公开确切数字**（网上流传 100/200/500 封都有，
  说明是按账号动态判定的）。所以"官方没说"≠"没事"，而是你根本不知道自己离红线多远。
- 一秒一封 = **一小时 3600 封**。这不是"会不会被发现"，是头几封之后就会被拦。
- 公开有人反馈：**一连发 9 封，授权码当场失效**（报 `535 Login Fail`）。

### 被拦之后会怎样

| 情况 | 表现 | 恢复 |
|---|---|---|
| 临时限流（最常见） | 之后全部失败、提示「登录失败」 | 等 1~2 小时 |
| 授权码作废 | 突然报 535 登录失败 | 重新生成，可能要等一天 |
| 服务被停用 | SMTP 服务被关 | 要重新开通，最麻烦 |

**还有一笔更常见、也更亏的隐形损失：HR 那边压根收不到。**
收件人里很多是 163、企业邮箱，它们对"陌生发件人 + 短时间大量 + 带附件"判得更狠，
直接丢垃圾箱或拒收。**你这边显示「发送成功」，人家那边什么也没看见** ——
比被限流更常见，而且你完全察觉不到。

### 正确的做法

- 保持默认：**40 秒一封、一天 50 封**（这是刻意压到很低的，不是随便定的）
- 想多投几十家 → **分几天投**，而不是把间隔改小
- 一天 50～100 封是安全区，100 封以上随时可能被限
